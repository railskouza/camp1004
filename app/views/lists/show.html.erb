

  <h1><%= @list.title %></h1>

  <%= @list.description %>


<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>

<hr>
<ul>
	<%= render @list.items %>
</ul>


<%= form_for [@list, @list.items.build],  do |f| %>
	<%= f.text_area :body %><br>
	<%= f.submit %>
<% end %>